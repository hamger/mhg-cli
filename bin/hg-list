#!/usr/bin/env node
const chalk = require('chalk')

/**
 * Padding.
 */

console.log()
process.on('exit', () => {
  console.log()
})

/**
 * List repos.
 */

const templates = [{
  name: 'simple',
  description: 'webpack + babel-loader'
}, {
  name: 'vue',
  description: 'webpack + vue + vuex + hot reload + linting + testing + css extraction'
}, {
  name: 'pages',
  description: 'webpack + babel-loader + eslint + sass, for developing in multiple pages'
}, {
  name: 'package',
  description: 'webpack + babel-loader + eslint, for developing in javascript package'
}, {
  name: 'cms',
  description: 'webpack + vue + vuex + element-ui, for content management system'
}]

console.log('  Available official templates:')
console.log()
templates.forEach(repo => {
  console.log(
    '  ' + chalk.yellow('★') +
    '  ' + chalk.blue(repo.name) +
    ' - ' + repo.description)
})
