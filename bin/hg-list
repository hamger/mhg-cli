#!/usr/bin/env node

const logger = require('../lib/logger')
const chalk = require('chalk')

/**
 * Padding.
 */

console.log()
process.on('exit', () => {
  console.log()
})

/**
 * List repos.
 */

let templates = [{
  name: 'simple',
  description: 'A simple Webpack + babel-loader setup for the develop with ES6.'
}, {
  name: 'vue',
  description: 'A full-featured Webpack + vue-loader setup with hot reload, linting, testing & css extraction.'
}]

console.log('  Available official templates:')
console.log()
templates.forEach(repo => {
  console.log(
    '  ' + chalk.yellow('★') +
    '  ' + chalk.blue(repo.name) +
    ' - ' + repo.description)
})
